### 문제풀이

문제 예시<br/>
[[2,2],[1,4],[3,2],[3,2],[2,1]]<br/>
-> [ [ 3, 2 ], [ 3, 2 ], [ 2, 1 ], [ 2, 2 ], [ 1, 4 ] ]<br/>

1. 근무태도는 내림차순 정렬<br/>
2. 동료평가 점수는 오름 차순 정렬<br/>

근무태도가 같으면 동료 평가 점수는 무조건 오름차순 정렬이다.<br/>
만약 내 앞의 동료 평가 점수가 나보다 높은 사람이 한 사람이라도 있으면, 근무 태도도 나보다 무조건 높다.<br/>
- 근무태도 동점자의 경우 동려할 필요x(동료 평가가 무조건 같거나 높으므로)<br/>

```js
if (score[1] < maxScore) {
  // 탈락대상
  if (score === wanho) return -1;
}
```
탈락대상이 아니면, 원호의 rank를 수정해야함
현재 점수가 원호의 점수 합 보다 크면 원호의 등수가 하나 밀림
```js
 else {
      // 인센대상
      maxScore = Math.max(maxScore, score[1]);
      if (score[0] + score[1] > wanhoSum) {
        answer++;
      }
    }
```
