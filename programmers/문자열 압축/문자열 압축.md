### 문제풀이

문자열을 자르는 단위(cnt)는 1개부터 최대 문자열(s)의 길이 까지다.<br/>
문자열 s를 cnt단위로 자르면서, 이전 repeat과 같다면 최종문자열에 추가하고, repeat을 다음 cnt개의 문자열로 초기화한다.<br/>
i가 s의 길이이상에 도달하면서 while문이 종료되었을 때, 아직 추가 되지 않은 나머지 `count+repeat`을 마저 추가하고 그 길이를 계산한다.