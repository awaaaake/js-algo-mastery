### 문제풀이

**투포인터**<br/>

- for문으로 start지점을 이동<br/>
- 고정된 start에대해, sum < m이면 while로 end를 계속 전진(필요한 만큼 확장)<br/>
- 검사해서 m이면 카운트<br/>
- start 증가<br/>
  -> 확장 -> 검사 -> 축소<br/>

**시간복잡도**
start가 n번, end가 n번 움직임 -> O(n) + O(n) = O(2n) -> O(n)<br/>
