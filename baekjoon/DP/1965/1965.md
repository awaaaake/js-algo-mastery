### 문제풀이

최장증가 부분 수열의 길이<br/>

```js
for (let i = 1; i < N; i++) {
  for (let j = 0; j < i; j++) {
    if (arr[i] > arr[j]) {
      dp[i] = Math.max(dp[i], dp[j] + 1);
    }
  }
}

console.log(Math.max(...dp));
```
각 i번째 순서에서 끝나는 증가하는 부분수열의 최대길이<br/>
- 이중 for문<br/>
- 기존의 dp값과 비교하며 갱신한다⭐<br/>