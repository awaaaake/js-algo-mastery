### 문제풀이

i부터 j까지의 구간합 = `j`까지의 누적합 - `i-1`까지의 누적합<br/>
누적합을 구하는 방식
1. reduce
2. for문
```js
const cumsum = new Array(arr.length + 1).fill(0);
arr.forEach((v, i) => {
  cumsum[i + 1] = cumsum[i] + v;
});
```
