### 문제풀이

구해야하는 값 : 정수 상한액의 최댓값(가능한 한 최대의 예산을 사용해야하기 때문)<br/>
- 이분탐색(1 이상 100,000 이하)<br/>
- 조건<br/>
    - `각 지방의 예산 요청 금액 >= 상한액` 이면 상한액을, `각 지방의 예산 요청 금액 < 상한액`이면 요청값을 누적한 값을 구한다.
    - 누적값이 총 예산 M을 넘지 않으면 정답으로 설정 후, left를 mid+1로 늘린다.